on:
  push:
    branches:
      - main
    
name: Deploy website on push

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Use Node.js 22.13.1
        uses: actions/setup-node@v2
        with:
          node-version: '22.13.1'
        
      - name: 🔨 Build Project
        run: |
          npm install
          npm run build
      
      # --- HARDCODED CREDENTIALS FOR SFTP DEPLOYMENT ---
      - name: 📂 Sync files to SFTP server
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          # HARDCODED Username
          username: 'urgroupc'
          
          # HARDCODED Server IP Address (Fixes "Could not resolve hostname")
          server: '102.212.246.99'
          
          # SFTP Port
          port: 22 
          
          # HARDCODED Password (!!! CRITICAL SECURITY RISK !!!)
          password: '3i@g8rVzR7!('
          
          # Local path to upload from
          local_path: './build/*'
          
          # Remote destination path
          remote_path: '/home/urgroupc'
          
          delete_remote_files: true